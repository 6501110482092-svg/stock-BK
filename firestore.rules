rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /stockItems/{id} {
      allow read, write: if true;
    }
    match /logs/{id} {
      allow read, write: if true;
    }
  }
}
